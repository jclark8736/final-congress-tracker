import React from 'react'
import { Card, Button, Header, Container } from 'semantic-ui-react'
const BillCard = (props) => {
    return (
       <Container style={{backgroundColor:'antiquewhite', border:'navy', padding:'3em', margin:'1em'}}>
           <Header>BILL CARD</Header>
       </Container>
    )
}

export default BillCard
